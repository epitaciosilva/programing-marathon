#include <iostream>
#include <string>
#include <map>
#include <algorithm>
#include <vector>

using namespace std;

int ready[100];
int vet[100];
int length[100];

struct item {
    int num, soma;
};

int main() {
    int n, cont = 0;
    cin >> n;

    vector<item> length;
    

    for(int i = 0; i < n; i++) {
        cin >> vet[i];
    }
    return 0;
}